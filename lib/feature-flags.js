/**
 * Feature flags for gradual rollout.
 */
export const FEATURE_FLAGS = {
  /**
   * When true, enriched videos are persisted to a videos table for analytics.
   * Default: false. Enable when ready to add watch analytics features.
   */
  persistVideos: false,
};
